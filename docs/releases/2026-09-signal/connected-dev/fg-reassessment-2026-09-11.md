# PR F (#114) / PR G (#115) reassessment against Daniel's confirmed authority model (2026-09-11)

Founder directive: canonical Alpha admission belongs to Daniel's backend; F/G must not be rebased and merged as written. Classification of every runtime component introduced by F/G (heads `133f6a5` / `8e5eaee`), returned **before** any rewrite. Vocabulary: REMOVE · REPLACE WITH BACKEND PROJECTION · KEEP AS NON-AUTHORITATIVE UI LOGIC · WAIT FOR DANIEL CONTRACT.

| Component (F/G)                                                                                                                                                                                    | What it does today                                                                  | Classification                                                                                                                        | Rationale                                                                                                                                                                                                                                                                                    |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `lib/compliance/alpha-admission.ts` — `evaluateAlphaAdmission` (pure rule `refi.alpha.admission.v1`)                                                                                               | computes canonical admission from ten prerequisites                                 | **REPLACE WITH BACKEND PROJECTION** (as authority); at most **KEEP AS NON-AUTHORITATIVE UI LOGIC** for an "onboarding readiness" hint | The backend now combines trusted results + identity + membership + consents + holds and persists the decision, reasons and rule version. The frontend rule cannot be canonical. A readiness hint may remain only if it is never surfaced as "admitted" and never gates anything server-side. |
| `alpha-admission.ts` — `gatherAlphaAdmissionPrerequisites`                                                                                                                                         | reads backend onboarding/eligibility/consents, ReFi profile, trusted KYC, ReFi hold | **WAIT FOR DANIEL CONTRACT**                                                                                                          | Inputs become the backend's concern; a frontend gatherer would only survive for the readiness hint. The cohort read is superseded (below).                                                                                                                                                   |
| `alpha-admission.ts` — `ALPHA_COHORT_POSITIVE_STATES` / `cohortSignalFrom` (PR G)                                                                                                                  | positive cohort allowlist over `OnboardingStatus.state`                             | **REMOVE** once the membership object exists (now: **WAIT FOR DANIEL CONTRACT**)                                                      | Daniel rejected the onboarding-state allowlist; dedicated backend-owned membership is coming. Nothing may infer membership from onboarding state once the contract exists.                                                                                                                   |
| `alpha-admission.ts` — `ensureAlphaAdmissionEvaluated` / `admission-hook.ts` and its call sites (KYC evaluation, webhook, consent, profile, onboarding read, admission read, brokerage connection) | orchestrates gather → evaluate → atomic record at every boundary                    | **REPLACE WITH BACKEND PROJECTION**                                                                                                   | The backend evaluates automatically and applies updates/revocations/expiries without another login; the frontend's job is to submit evidence (attestation, consents) and read the projection. The call sites that submit evidence stay; the "evaluate admission here" semantics go.          |
| `prototype-store/entities/alpha-admission.ts` — admission record, history, provenance, `recordAdmissionEvaluation` (atomic in G)                                                                   | persists canonical admission + audit history in frontend Firestore/prototype store  | **REMOVE**                                                                                                                            | Frontend Firestore is not authoritative admission state; the backend persists decision, reasons, rule version and evidence history. Keeping a local copy would create a second source of truth.                                                                                              |
| `config/backing.ts` — `alpha-admission` backing entry                                                                                                                                              | durable backing for the above                                                       | **REMOVE** with the entity                                                                                                            | —                                                                                                                                                                                                                                                                                            |
| `GET /api/v1/investor/admission` route + manifest entry                                                                                                                                            | evaluates on read and returns the frontend record                                   | **REPLACE WITH BACKEND PROJECTION**                                                                                                   | Keep a read route only as a thin pass-through of the backend admission projection (state, reasons, rule version, timestamps) once the contract exposes it; must be read fresh on each authoritative use, never cached in the session (continuous backend effects, founder §17).              |
| `store/types.ts` `KVStore.update` + prototype per-key lock + durable `runTransaction` + test seam (PR G)                                                                                           | generic atomic transactional update in the store abstraction                        | **KEEP AS NON-AUTHORITATIVE UI LOGIC** (infrastructure primitive; reusable)                                                           | Independent of admission; useful for any frontend-owned exactly-once transition (e.g. KYC webhook idempotency). Founder accepted the design technically. Ship in a small standalone PR if wanted; not tied to admission.                                                                     |
| Webhook route change (evaluate admission after an applied final decision)                                                                                                                          | best-effort admission evaluation                                                    | **REMOVE** (the admission call); keep durable-record-before-2xx                                                                       | Provider result recording stays; the backend derives admission from the attestation/evidence, not from a frontend evaluation.                                                                                                                                                                |
| Onboarding aggregate `admission` field                                                                                                                                                             | exposes the frontend record                                                         | **REPLACE WITH BACKEND PROJECTION**                                                                                                   | Render the backend state (Daniel's vocabulary) once delivered.                                                                                                                                                                                                                               |
| Brokerage connection route hook                                                                                                                                                                    | re-evaluates admission before connecting (no deny)                                  | **REMOVE**                                                                                                                            | Admission precedes brokerage connection in the journey, but the backend enforces its own ownership/action protections; trading authorization never blocks first connection (Daniel §5). No frontend admission gate here.                                                                     |
| Contract-assertion sections for F/G (admission matrix, cohort matrix, atomicity, boundaries)                                                                                                       | tests of the above                                                                  | **REMOVE** those tied to frontend canonical admission; **KEEP** the `KVStore.update` atomicity tests if the primitive ships           | —                                                                                                                                                                                                                                                                                            |
| CONTROL_MATRIX IB-20, readiness rows, `decision-alpha-admission-automatic.md`                                                                                                                      | documents frontend-owned automatic admission                                        | **REPLACE** wording: ReFi owns compliance decisions + evidence; backend owns canonical admission                                      | Docs-only; part of the re-scope.                                                                                                                                                                                                                                                             |

## What the frontend WILL own after the contract lands (no code yet)

Submit trusted evidence (existing attestation with provider = Socure and the evidence reference/hash; evaluation ids, workflow details and component outcomes stay behind the reference); present disclosures before consent and record version/hash-bound consents (existing); read the backend admission projection and render Daniel's states (admitted / held / suspended / revoked in his vocabulary) and reasons, re-reading on every authoritative use rather than trusting a cached session claim; support "complete broker connection later"; show brokerage connection, PAPER/LIVE selection and portfolio subscription as outstanding setup steps; never call any `setAdmitted` equivalent (none will exist).

## Disposition proposal (founder decision F1)

Close F and G as superseded once the addendum defines the membership object and the admission projection, and open a narrow replacement: (1) backend-projection read route + UI state; (2) removal of the frontend admission entity; (3) optional standalone PR for the `KVStore.update` primitive. Until then F/G stay open and untouched as reference. No rewrite is started under this directive.
