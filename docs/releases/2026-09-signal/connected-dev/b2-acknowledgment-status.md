# B2 — acknowledgment / continuation status (frontend side)

Status date: 2026-09-10 (founder-approved B2 slice). Contract: v1.1.0-alpha.3.

| Item                                                                                                                                                                                                                                                             | State                     | Proof                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Client retains validated `error.continuation` on `InvestorApiError`                                                                                                                                                                                              | **DONE**                  | `packages/api-clients/src/investor-api/errors.ts`, `client.ts`; `investor-api-alpha3.test.ts`                                                                                                                                                                    |
| Preference BFF continuation pass-through                                                                                                                                                                                                                         | **DONE**                  | `apps/web/app/api/v1/investor/preferences/route.ts` (challenge echoed with `mutationApplied:false`, `confirmPath`)                                                                                                                                               |
| Preference user confirmation flow (initial PATCH → 409 → exact disclosure shown → explicit confirm → consent for the exact tuple → same fields + `continuation_ref` + `consent_receipt_id` + current If-Match + NEW key → 202 `APPLIED` → authoritative re-read) | **DONE (fixture-proved)** | `lib/investor-api/preference-confirmation.ts`, `lib/investor-api/acknowledgment.ts`, durable `acknowledgment-challenge` entity, `POST /api/v1/investor/preferences/confirm`; contract sections "preference confirmation (B2) …" ×4 and "acknowledgment routes …" |
| Disconnect acknowledgment adapter and confirmation flow                                                                                                                                                                                                          | **DONE (fixture-proved)** | `brokerage-maintenance.ts` disconnect/confirm, `DELETE /api/v1/investor/broker/connection/[id]`; contract sections "brokerage disconnect …" ×2                                                                                                                   |

Remaining: genuine connected acceptance of both flows (remote traffic BLOCKED). Contract observations for Daniel: `brokerage_mutation` declares neither `ACCOUNT_AUTHORIZATION_REQUIRED` (403) nor `ACKNOWLEDGMENT_BINDING_INVALID`; the frontend treats either on disconnect as a contract mismatch (fail closed). No UI screen for the confirmation prompt exists yet; the BFF contract for it is complete.
