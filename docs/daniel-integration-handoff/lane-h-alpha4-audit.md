# Lane H — strategy / subscription / allocation against v1.1.0-alpha.4

**Date:** 2026-09-13 · **Base:** `daniel-handoff/integration` at `019a6bf`
(alpha.4 is the contract authority) · Read-only audit; 402/402 client tests
and conformance validate + self-test pass on this head.

Classification: `CERTIFIED` · `DEFECT` · `DEBT` · `DANIEL-ASK` · `FOUNDER-DECISION`.

## H1 — recommendation detail compatibility debt

| id   | class              | evidence                                                                                                                                                                                                                                                                          | action                                                                                                                                                                           |
| ---- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H1-1 | DEBT (contained)   | `recommendations.ts` leaves `lastEvaluatedAt: ""`, `policyVersion: ""`, `reasonCodes: []` on the compatibility view — never fabricated; pinned by `alpha4-adoption.test.ts`.                                                                                                      | Keep the no-fabrication rule; fix the render.                                                                                                                                    |
| H1-2 | CERTIFIED          | alpha.4 `Recommendation` requires only `as_of_time`, `fresh_until`, `expires_at`, `freshness_status`, `updated_at` — no evaluation time or policy version. `sourceAsOf ← as_of_time` is a named field.                                                                            | None.                                                                                                                                                                            |
| H1-3 | DEFECT (cosmetic)  | Detail page renders `Last evaluated` = "—" and `Freshness policy` = an empty value under a live label.                                                                                                                                                                            | H-PR1 — delete both fields, or remap on detail only to `canonical.updated_at` / `canonical.lineage.policy_version` under honest labels (never as "freshness"). **Founder F-H3.** |
| H1-4 | DEFECT             | Detail page renders `freshness.reasonCodes` (structurally always empty); the real alpha.4 `reason_codes` (the array FUNDING.md says carries `PORTFOLIO_CAPITAL_BELOW_MINIMUM`) is projected and rendered nowhere.                                                                 | H-PR1 — render `rec.reasonCodes`; drop the dead block.                                                                                                                           |
| H1-5 | DANIEL-ASK (D-A8a) | `$defs.Freshness` exists in alpha.4 with all seven fields required — and is referenced by nothing (`$ref` count 0 in schemas and openapi). Specified and attached to nothing.                                                                                                     | Ask: attach or remove.                                                                                                                                                           |
| H1-6 | DEFECT (case)      | `lifecycle_status` / `freshness_status` are open strings with no enum anywhere. Examples: lowercase `current`/`fresh`; demo world: `CURRENT`/`SUPERSEDED`/`BLOCKED`. `data-rec-status` emits the raw value, so two e2e suites assert opposite casings against the same attribute. | H-PR2 — normalise the attribute and both suites.                                                                                                                                 |
| H1-7 | DEFECT (tone)      | `freshnessTone` matches `"fresh"` without normalising while `statusTone` upper-cases. alpha.4's own examples mix `fresh` (recommendation) and `FRESH` (position, template). An uppercase `FRESH` recommendation renders unbadged.                                                 | H-PR2 — `toLowerCase()` in `freshnessTone`.                                                                                                                                      |
| H1-8 | DANIEL-ASK (D-A8b) | The casing split is inside Daniel's own examples, not a frontend assumption.                                                                                                                                                                                                      | Ask: enum, or explicit "open, case-insensitive".                                                                                                                                 |

## H2 — `template_id` fan-out

Constants: `RECOMMENDATION_LIST_MAX_PAGES = 4`, `CONTRACT_MAX_PAGE_SIZE = 100`, detail groups of 4.

| id   | class              | evidence                                                                                                                                                                                                                                                                                               | action                                                                   |
| ---- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ |
| H2-1 | DEBT (quantified)  | Worst case 400 items → up to 400 detail fetches in **100 sequential rounds of 4** → **404 upstream calls and ~100 × RTT** per one BFF list request (~5 s at 50 ms RTT, ~10 s at 100 ms).                                                                                                               | Accept only until D-A8c lands.                                           |
| H2-2 | DEBT (common case) | Template identity on a summary comes only from `funding_assessment.input_versions.template_id`; FUNDING.md says historical assessments are `null` and never recomputed — so most rows of any paged list fan out. The demo world proves it: all three recommendations carry `funding_assessment: null`. | Same.                                                                    |
| H2-3 | DEFECT             | `Promise.all` over each group: one failed detail fetch rejects the group, propagates, and the route answers `{ items: [] }` with an upstream state — a single transient 503 on one of 400 reads **blanks the entire list**.                                                                            | H-PR3 — `Promise.allSettled`; a failed detail leaves `templateId: null`. |
| H2-4 | CERTIFIED          | `Object.assign` mutates only the per-request view objects; no shared/cached state; ordering preserved.                                                                                                                                                                                                 | None.                                                                    |
| H2-5 | DEBT               | No cache layer: `bffRead` sets no `revalidate` / `Cache-Control`; the route declares no segment config. Every poll and every `recommendation.updated` invalidation replays the full fan-out.                                                                                                           | Do not paper over with a cache; fix the contract (D-A8c).                |
| H2-6 | DANIEL-ASK (D-A8c) | `RecommendationSummary` already carries a lineage-derived field (`output_fingerprint`, required), so it is not lineage-free by design; `template_id` is simply missing. **This is the clean contract fix.**                                                                                            | Ask wording below.                                                       |

## H3 — funding assessment

| id   | class     | evidence                                                                                                                                                                                                                                                                                                                                     | action                                                                                                  |
| ---- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| H3-1 | CERTIFIED | Historical `null` → notice state `unavailable`, never sufficient (pinned).                                                                                                                                                                                                                                                                   | None.                                                                                                   |
| H3-2 | CERTIFIED | INSUFFICIENT clears only on a newer, `current`, `fresh`, unexpired `SUFFICIENT`; stale / superseded / null / incomplete only age the active notice; same-time tie cannot clear; an aged-out SUFFICIENT reverts to active. Status comparisons are case-normalised (the right hedge for H1-6). Pinned.                                         | None.                                                                                                   |
| H3-3 | CERTIFIED | No recommendation field is execution authority: `execution_eligible` (`const: false`), `content_status`, `funding_assessment` never appear in `account-actions.ts` or any allocation route; the only gate is `AccountAuthorization.status === "AUTHORIZED"`. Assertions "economic gating…" and "backend refusals keep their contract code…". | None.                                                                                                   |
| H3-4 | CERTIFIED | `recommendation.updated` SSE maps to query invalidation only; the payload's `reason_codes` are never read; notices are rebuilt server-side from persisted recommendations — exactly FUNDING.md's rule; a replayed older event cannot overwrite newer evidence.                                                                               | None.                                                                                                   |
| H3-5 | DEBT      | `fundingNotices` / `fundingComplete` are produced, typed on the route and the hook — and **rendered by no component**. FUNDING.md §2 requires a persistent notice with amounts and limiting constituents on INSUFFICIENT. Today an INSUFFICIENT assessment is silently discarded at the UI boundary.                                         | H-PR4 — render (decimal strings only, never `Number()` on money) or defer explicitly. **Founder F-H1.** |
| H3-6 | DEBT      | `PORTFOLIO_CAPITAL_BELOW_MINIMUM` is hardcoded as the only condition and never read from `reason_codes`. Correct today; will mislabel if a second condition is added.                                                                                                                                                                        | Note.                                                                                                   |

## H4 — allocation

| id    | class     | evidence                                                                                                                                                                                                                                                                                                                                                                                       | action                                                                                                                                       |
| ----- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| H4-1  | CERTIFIED | `allocation_percent` is a **fraction in (0, 1]** despite its name: pattern `^(?:0\.(?:0*[1-9][0-9]*)\|1(?:\.0+)?)$` on `AllocationPreview`, `AllocationPreviewRequest`, `AccountActionRequest.parameters`, `AccountMembership`. The BFF mirrors the pattern verbatim (`ALLOCATION_PERCENT_PATTERN`) at all three routes; no unit conversion on the write path. MIGRATION.md item 4 agrees.     | None; the name is a contract wart (D-A8d).                                                                                                   |
| H4-2  | DEFECT    | Home page renders the membership fraction as `(Number(x) * 100).toFixed(0)%` — binary float and whole-percent rounding on a canonical decimal string (`0.125` → "13%"). The exact converter already exists (`fractionToPercent`).                                                                                                                                                              | H-PR5 — use `fractionToPercent`.                                                                                                             |
| H4-3  | CERTIFIED | Limits are backend-owned: `feasible`, `effective_min_order`, `reason_codes`, `funding_assessment` are required on the preview and never recomputed by the BFF (0 non-contract hits); `allocationPreviewId` is required on join/update so an unbound join is rejected before any upstream call. Pinned.                                                                                         | None.                                                                                                                                        |
| H4-4  | CERTIFIED | P1C surface: `bounds === null` → `bounds_unknown`, refuse to validate rather than invent 0–100/1% defaults.                                                                                                                                                                                                                                                                                    | None.                                                                                                                                        |
| H4-5  | CERTIFIED | `createAccountAction` in alpha.4 carries no consent header and no 403; consent for economic verbs is a **backend** decision (`COMPLIANCE_ATTESTATION_REQUIRED`). Where the frontend owns a tuple (consent recording, preference confirmation, disconnect, P1C panel) it is exact and pinned.                                                                                                   | None.                                                                                                                                        |
| H4-6  | DEBT      | `createAccountAction` never sends the optional `If-Match`; determinism rests on the idempotency key. Not a hole today.                                                                                                                                                                                                                                                                         | Note.                                                                                                                                        |
| H4-7  | CERTIFIED | Separate from admission: the setup gate reads two distinct backend words and never conflates them; allocation gating reads `AccountAuthorization` only, never `OnboardingStatus`.                                                                                                                                                                                                              | None.                                                                                                                                        |
| H4-8  | CERTIFIED | Separate from commercial entitlement: `Stripe` / `entitlement` — 0 hits in `apps/web`.                                                                                                                                                                                                                                                                                                         | None.                                                                                                                                        |
| H4-9  | CERTIFIED | Stage separation: the four allocation verbs are `AUTOMATED_ALPHA_ONLY`; `bffMutate` refuses before body parsing at the signal stage with a policy receipt.                                                                                                                                                                                                                                     | None.                                                                                                                                        |
| H4-10 | DEBT      | Two disjoint allocation models with no wiring: the alpha.4 allocation routes have **no UI caller**; the only subscription UI (P1C `SubscriptionPanel`) uses percentage-point numbers on a fixture-only adapter whose transport is `transport_not_implemented`. Nothing is mis-sent today; a naive transport adapter sending `25` would be rejected at the zod boundary (fails closed, loudly). | No PR now. Hard requirement on the future transport adapter: exact base-10 points→fraction conversion, never float `/100`. **Founder F-H2.** |

## Narrow PRs

| PR    | Scope                                                                                                                               | Dependency   |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| H-PR1 | Detail page: remove or remap `Last evaluated` / `Freshness policy`; render `rec.reasonCodes`; drop the dead freshness-reason block. | Founder F-H3 |
| H-PR2 | Normalise `data-rec-status` casing; `freshnessTone` lower-cases; align both e2e suites.                                             | none         |
| H-PR3 | `Promise.allSettled` in the template fan-out; failed detail ⇒ `templateId: null`, list survives.                                    | none         |
| H-PR4 | Render funding notices (decimal strings; no `Number()` on money).                                                                   | Founder F-H1 |
| H-PR5 | Home page allocation display via `fractionToPercent`.                                                                               | none         |

H-PR2, H-PR3 and H-PR5 **merged as #158** (head `c4cdea6`, CI run 34802196817,
merge `57680cf`). F-H1 and F-H3 were **answered 2026-09-13** (render the funding
notice; delete the two unsupported fields, no relabelling), so H-PR1 and H-PR4
are unblocked. F-H2 answered: alpha.4 BFF allocation operations are canonical.

## Daniel asks (exact wording)

**D-A8a — attach or remove the orphaned `Freshness` schema.** "`v1.1.0-alpha.4/schemas.json` defines `$defs.Freshness` (`source_as_of`, `last_evaluated_at`, `fresh_until`, `expires_at`, `freshness_status`, `freshness_policy_version`, `freshness_reason_codes`, all required) and `openapi.json` publishes it under `components.schemas`, but neither file contains a single `$ref` to it. `Recommendation` and `RecommendationSummary` carry flat `freshness_status` / `fresh_until` / `expires_at` and no evaluation time or policy version. Please either (a) `$ref` `Freshness` from the recommendation shapes or (b) delete it from the package. We render blanks rather than fabricate, and will remove those fields on (b)."

**D-A8b — pin the `lifecycle_status` and `freshness_status` vocabularies.** "Both are `{"type": "string"}` with no `enum`, and alpha.4's `examples.json` is inconsistent in case (`RecommendationEnvelope.freshness_status = "fresh"` vs `AccountPositionEnvelope` / `TemplateEnvelope` `= "FRESH"`). We need either (a) an `enum` per field per shape, or (b) an explicit statement that both are open, case-insensitive sets — in which case we normalise on read and never key UI or test selectors off the raw value."

**D-A8c — expose `template_id` on `RecommendationSummary`.** "`listAccountRecommendations` returns `RecommendationSummary`, which has no `template_id` and no `lineage`. Template identity is recoverable only from `funding_assessment.input_versions.template_id`, and FUNDING.md states historical assessments are `null` and never recomputed — so for any list containing history most rows have no template identity. Our BFF fills the gap with bounded `getAccountRecommendation` calls: worst case 4 pages × 100 items = 400 detail fetches in 100 sequential rounds of 4 — 404 upstream calls for one list request, with no cache layer. `RecommendationSummary` already carries a lineage-derived field (`output_fingerprint`, required), so adding `template_id` (required, same pattern as `lineage.template_id`) is consistent with the shape's design. We will delete the fan-out the day it lands."

**D-A8d (minor) — `allocation_percent` is a fraction.** "`allocation_percent` on `AllocationPreview`, `AllocationPreviewRequest`, `AccountActionRequest.parameters` and `AccountMembership` uses `^(?:0\.(?:0*[1-9][0-9]*)|1(?:\.0+)?)$`, i.e. (0, 1]. The name reads as percentage points — a live unit hazard of exactly the kind alpha.3→alpha.4 already corrected for turnover. If renaming is off the table, a one-line `description` stating 'decimal fraction in (0, 1], not percentage points' on each occurrence would close it."

## Founder decisions

- **F-H1 — ship the funding-notice UI or defer explicitly.** FUNDING.md §2 requires a persistent notice with amounts and limiting constituents on INSUFFICIENT; projection, route field and hook field exist and are test-pinned; no component renders them. (a) build H-PR4 (new investor-facing copy about capital shortfall — compliance-relevant), or (b) a written, dated deferral. The notice is informational only: it gates nothing in our code.
- **F-H2 — which subscription surface is real.** alpha.4 allocation routes (fraction strings, zero UI callers, automated-Alpha only) versus the P1C `SubscriptionPanel` (percentage-point numbers, fixture-only, transport not implemented). Neither transacts today. Decide which Lane H carries forward before anyone writes the transport adapter; building both is how H4-10 becomes a live money bug.
- **F-H3 — H1-3 disposition.** Delete `Last evaluated` / `Freshness policy` from the detail page (smaller, reversible; recommended) or remap to `updated_at` / `lineage.policy_version` under new labels. Re-add only against a `$ref`'d contract field (D-A8a).
