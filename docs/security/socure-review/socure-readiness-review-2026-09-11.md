# Socure readiness static review — 2026-09-11 (main `abb73d9`)

| Review              | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Evidence                                                                                                     |
| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| PII retention       | **No exception.** The evidence record (`lib/kyc/evidence.ts`) carries ids, decisions, coarse component statuses (`documentVerification` is a status enum, not an image), timestamps and provenance; `KYC_EVIDENCE_FORBIDDEN_KEYS` lists given/family name, DOB, national id, email, phone, address, selfie, document image, tags, decision tags, reason codes, score, notes, DI token, API key. The evaluation entity never writes an identity field. The only provider value persisted beyond ids is the DocV transaction token (opaque, per-evaluation, needed to launch capture for the owner; not PII). Raw Socure payloads, detailed scores and tags are parsed and discarded (asserted). | contract sections "socure: fake-client evaluation … never PII", "attestation evidence …", `pii-inventory.md` |
| Logging / redaction | **No gap.** No `console`/logger call in `lib/kyc`, the KYC routes, the webhook route, the DI/DocV seams or the compliance modules; provider error messages carry kind/status only; keys, tokens and identity fields are never interpolated into strings (the only `JSON.stringify(request)` is the HTTPS body sent to Socure, not a log). Existing assertion section "kyc logging hygiene" already covers this; no new tests added.                                                                                                                                                                                                                                                            | contract section "kyc logging hygiene…"                                                                      |
| Secret boundary     | **Correct.** Browser receives only `NEXT_PUBLIC_SOCURE_SDK_KEY`; `SOCURE_API_KEY`, `SOCURE_WEBHOOK_BEARER_TOKEN` and `SOCURE_WORKFLOW_NAME` are server-only, read from the server env schema, never referenced by a client component; no `NEXT_PUBLIC_` variant of a server secret exists (asserted); the browser payload schema refuses provider controls.                                                                                                                                                                                                                                                                                                                                    | contract sections "socure: configuration…", "kyc evaluation input: strict browser payload…"                  |
| Defects found       | **None.** No runtime PR is required.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | —                                                                                                            |
