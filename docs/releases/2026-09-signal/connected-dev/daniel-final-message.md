# Message to Daniel (founder-approved, ready to send)

Daniel — we've settled the identity/KYC/admission architecture on our side and want to lead with the proposal rather than ask you to design it. Please answer each numbered item with **CONFIRM**, **PROPOSE EXISTING EQUIVALENT**, or **ADDENDUM REQUIRED**.

**Target flow.** Stytch authentication → explicit Alpha invitation (backend cohort membership) → profile / disclosures / consents → Socure KYC + Fraud + Watchlist (DocV when required) → automatic ReFi Alpha admission on a final trusted Socure ACCEPT → your backend opens/links the Alpaca brokerage account (Broker API) → Alpaca approval → account sync → AccountAuthorization → Signal → Managed Paper. Three separate decisions: Socure ACCEPT (ReFi KYC satisfied), Alpaca APPROVED/ACTIVE (brokerage account operational), AccountAuthorization (economic product access). None substitutes for another.

**1. Cohort authority (positive signal).** Long-term: a dedicated backend-owned `ClosedAlphaMembership` (user ref, cohort ref, positive membership state, invited_at, optional expiry, revocation). Missing, expired, revoked or unknown fails closed. For this Alpha, if the object cannot land before launch, please explicitly bind the existing post-invitation onboarding states as the temporary positive cohort set: `INVITED`, `IDENTITY_VERIFIED`, `PROFILE_REQUIRED`, `DISCLOSURE_REQUIRED`, `CONSENT_REQUIRED`, `READY` (WAITLISTED / INELIGIBLE / SUSPENDED / missing / unknown = not eligible). CONFIRM, or give us the correct positive field. We will not infer approval from silence.

**2. Canonical Alpha admission.** ReFi owns the automatic admission policy (positive cohort + authenticated identity + backend identity mapping + eligibility + complete advisory profile + disclosures delivered + consents accepted + final trusted Socure ACCEPT + no compliance hold = admitted; no routine human approval; human review exception-only). We propose your backend persists the canonical result: an admission object with user/account ref, state (conceptually ADMITTED / HELD / REVOKED — your existing equivalent is fine), rule version, decision timestamp, trusted-KYC evidence ref, cohort-membership ref, profile/consent refs, provenance. The compliance attestation stays **evidence, not admission**; AccountAuthorization stays separate and downstream.

**3. Attestation payload.** Provider-neutral references only: provider label, Socure `eval_id`, workflow name/version, final decision, completion timestamp, ReFi evidence ref/hash, coarse component outcomes. No images, selfies, raw SSN, scores/tags or raw payload. CONFIRM the vocabulary or state yours.

**4. Alpaca onboarding.** Your backend owns Broker API account opening, status events and Alpaca credentials; ReFi collects applicant fields and submits through the BFF to you; you expose a read-only account/application projection preserving at least SUBMITTED, APPROVAL_PENDING, ACTION_REQUIRED, APPROVED, ACTIVE, REJECTED; ACTIVE = product-ready. Per-user Trading API keys are transitional only. CONFIRM / ADDENDUM.

**5. Contract details.** `brokerage_mutation`: no `ACCOUNT_AUTHORIZATION_REQUIRED` for connection-lifecycle operations; `ACKNOWLEDGMENT_BINDING_INVALID` where a brokerage mutation uses an acknowledgment continuation; ideally per-operation error profiles. B3: two independent invited happy-path identities plus one negative/review identity, each with its own account, membership, Socure evaluation and Alpaca PAPER account. B1: separate service identities/audiences for Identity and Investor API, native Cloud Run auth, separate ES256 bridge and Investor assertion keys/JWKS, no downloaded SA keys, exact revision pinning.

Items 1 and 2 gate our admission PRs; everything else can follow in an addendum. Full table: `daniel-target-architecture-proposal.md`.
