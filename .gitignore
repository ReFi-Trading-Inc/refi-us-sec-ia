# dependencies
node_modules
.pnpm-store

# build outputs
.next
dist
.turbo
*.tsbuildinfo

# env
.env
.env.local
.env.*.local
!.env.example

# generated — raw openapi-typescript output is ephemeral. The hand-maintained
# compatibility / shim layer lives at packages/api-clients/src/compat.ts.
packages/api-clients/src/generated/

# BFF prototype storage (dev-only, not a compliance system of record;
# see docs/bff-prototype-state-contract.md)
.refi-prototype-store/
apps/web/.refi-prototype-store/
apps/web/.refi-prototype-store-e2e/

# Evidence bundles are regenerated on demand from CI artifacts + the
# current commit. The manifest is deterministic given repo state, so
# committing individual dated runs adds churn without adding evidence.
# See scripts/assemble-evidence-bundle.ts and docs/alpha-gate-checklist.md.
artifacts/evidence-bundle/

# playwright
apps/web/playwright-report/
apps/web/test-results/

# misc
.DS_Store
*.log
coverage
.nyc_output
.vercel
.env*
