# Socure certification hardening — N1–N4 (2026-09-12)

Founder directive 2026-09-12 (narrowed after Daniel's review): A and D–L stay accepted; the delta is N1 workflow/DocV configuration verification, N2 normalized score/reason-code retention, N3 nullability hardening, N4 GET reconciliation for a missed final webhook (Scenario M), plus the DocV callback audit. Production RiskOS traffic remains NOT AUTHORIZED.

## N1 — Sandbox workflow verification (account-specific, via the RiskOS Sandbox MCP server, read-only)

Source: `list_usecases`, `list_workflows`, `list_testcases`, `integration_checklist` on `https://mcp.riskos.socure.com/sandbox` with the account MCP Server Key (kept only in Secret Manager `socure-mcp-key-sandbox`; never in chat, source or logs).

| Question                            | Evidence                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Is `consumer_onboarding` active?    | `list_workflows`: id `2ba07627-03ea-4c37-a480-44071e7c509b`, name `consumer_onboarding`, version `1.0.0`, `state: PUBLISHED`, `mode: LIVE`, display name "KYC & Watchlist Screening: Direct API Integration", fraudType `sc_onboarding`; 117 steps                                                                                                                                         |
| Document Request step present?      | ENRICHMENT step **"Socure Document Request - Default Flow"** → output `socure_doc_request_response` (error field `socure_doc_request_error`)                                                                                                                                                                                                                                               |
| Document Verification step present? | ENRICHMENT step **"Socure Document Verification - Default Flow"** → `socure_docv_response`; plus "Socure Image Request - Default Flow"                                                                                                                                                                                                                                                     |
| Conditional step-up logic present?  | CONDITION **"DocV StepUp?"** = OR of `Sigma_Synthetic_Step_Up`, `Graph_Intel_Step_Up`, `Sigma_Identity_Fraud_Step_Up`, `Phone_Risk_Step_Up`, `Email_Risk_Step_Up`, `Address_Risk_Step_Up`, DOB / name mismatch step-ups; CONDITION "DocV Bypass" reads `$request.data.custom.skip_docv`; CONDITION "10sec for Sandbox" gates on `socure_doc_request_response.environmentName == "Sandbox"` |
| Decision logic after DocV?          | CONDITIONs "Document Request Incomplete" (status ≠ SESSION_COMPLETE, request error, session expired, consent declined → REJECT), "Response Error?", "DocV Reject?" (decision contains reject/resubmit → REJECT tag "DocV Reject"); DECISION steps ACCEPT / REJECT ("High Risk Individual", "Verify Reject", "DocV Reject")                                                                 |
| Shape                               | Input → KYC/Fraud/Watchlist enrichments (Verify, Sigma Synthetic, GraphIntelligence + Sigma Fraud, Digital Intelligence, Email/Phone/Address Risk, Watchlist Plus) → TAG/RULE_SCORE/CONDITION step-up logic → Document Request → Document Verification → Image Request → decision logic → Decision. Matches the expected shape.                                                            |
| Test cases                          | `list_testcases`: exactly one — "Minimum PII Fields \| ACCEPT \| DI Error \| Email Risk Error \| Address Risk Error" (POST /api/evaluation). No REVIEW / REJECT case is exposed. Per Socure docs, workflow testing does not support asynchronous steps (DocV), so this absence does not prove the workflow lacks DocV; the workflow definition above does prove the path exists.           |
| Integration checklist               | 14 steps, all `isComplete: false` (account tracking only): keys, workflow id, security controls, Evaluation API, DI SDK, DocV SDK, webhook registration, event subscription, retry/error handling, decision routing, logging (eval_id/scores/reasonCodes), DocV step-up handling, Production switch, Feedback API                                                                          |
| Capture App Sandbox flow            | **NOT VERIFIED BY THIS SESSION** — the Capture App configuration (flow exists, published, Sandbox, SDK path not Hosted Flow, SDK key valid for the flow) is a dashboard-only surface not exposed by the MCP tools and requires a logged-in RiskOS session (founder / Socure session). Recorded as an open verification item; Production needs its own verification later.                  |
| Sandbox SDK key                     | `fcb67dc3-…` (public) is the Environment Variables SDK key of the Sandbox account; DI SDK sessions succeeded live in Scenario A with it; DocV SDK use is unproven until B.                                                                                                                                                                                                                 |

## N2 — Certification retention (scores / reason codes)

Implemented as `providerDetail` on the evaluation record (`apps/web/src/lib/kyc/socure/certification-detail.ts`, schema `refi.socure.certification-detail.v1`): eval_id, workflow, workflow_id, workflow_version, decision, status, sub_status, evaluation status, eval_at, decision_at, top-level score, reason_codes, decision_tags, tags, review_queues, and per-enrichment `{enrichmentName, enrichmentProvider, failed, scores[{name, version, value}], reasonCodes[]}`; captured from the evaluation response, from every finalizing webhook, and from GET reconciliation (`source`). Never the request or raw response. **Restricted**: server-side only; the session view is built field by field; the evidence record, attestation evidence, evaluate/reconcile/webhook responses and logs never carry it (asserted). Daniel's attestation is unchanged (provider, normalized state, evidence reference). Retention period: policy/contract question, not invented.

## N3 — Nullability

`socureEvaluationResponseSchema` / `socureEvaluationCompletedEventSchema`: informational fields (`score`, `notes`, `sub_status`, `tags`, `reason_codes`, `decision_tags`, `review_queues`, `eval_at`, `decision_at`, `workflow_id`, empty `environment_name`) are nullable/optional; `data_enrichments[]` items accept `response: null` (failed enrichment → recorded as `failed`, evaluation still parsed and decided by `decision`/status). Authority stays strict: `eval_id`, `decision` (enum), lifecycle status enums, `event_id`, `event_type`, `data`. Assertions cover each case and the refusals.

## N4 — Missed-webhook reconciliation

- Client: `getEvaluation(evalId)` → `GET /api/evaluation/{eval_id}` through the same request builder (pinned `X-API-Version`, timeout, correlation id); `include_input` never sent.
- Adapter: `reconcile(subject)` — non-terminal + has eval_id only; bounded backoff 30 s → 2 min → 10 min → 30 min → 60 min recorded on `record.reconcile`; `not_due` short-circuits without a provider call; still-paused keeps state; outage/mismatch → `provider_error` (retryable, never a rejection); terminal → `finalizeProviderResult` with synthetic event id `reconcile:<eval_id>:<decision>`.
- Single finalization: `applyWebhook` and `reconcile` both call `finalizeProviderResult` → `applyFinalProviderDecision` (one call site, asserted); a later real webhook for the same result is `idempotent_same_result` (no duplicate history/evidence), a conflicting one `conflict_flagged`.
- BFF: `POST /api/v1/investor/kyc/reconcile` (`reconcileKycEvaluation`, bffMutate, session-scoped, same-origin, Signal-allowed); the browser never calls RiskOS.
- Browser triggers: page resumed/refreshed with a non-terminal journey that has an evaluation, DocV `onSuccess` (after "captured"), relevant DocV `onError` outcomes.
- Stale-evaluation recovery model: user-initiated reconciliation now; a bounded server-side sweep for stale non-terminal evaluations (`reconcile.notBefore` elapsed, capture reported but no final webhook) is designed but not scheduled — to be enabled with B once a live asynchronous case exists.
- **Scenario M — PASS (FIXTURE DRIVEN)**: non-terminal under-review journey, no webhook, GET returns terminal ACCEPT → `passed`, final evidence, one history entry, audited synthetic event; late webhook → `idempotent_same_result`, no duplicate; REJECT via GET → `failed`; paused → `still_pending`; 503 → `provider_error` + backoff + `not_due`; wrong environment refused; terminal / no-eval → no provider call. To be repeated live with B.

## DocV callback audit

`launchDocumentCapture` (`app/_lib/kyc/docv-sdk.ts`) now wires `onProgress` (UX only, not persisted), `onSuccess` ("captured" → BFF `completeKycStepUp` → under_review → reconcile; never verification), `onError` (classified: launch/config, upload, interrupted, consent declined, transient; never a rejection; reconcile after terminal-capable outcomes). Launch failures are operational (`sdk_key_unconfigured`, `sdk_load_failed`, `sdk_error`, `duplicate_launch`, `invalid_token`) with safe retry and no KYC state change. Live callback behaviour remains unproven until B.

## Updated Production gates

A PASS · B PASS · C PASS · D–L accepted · M PASS (live repeat with B) · workflow DocV path verified (done, N1) · Capture App flow verified (open) · scores/reasonCodes retained (done) · nullability hardened (done) · DocV callbacks hardened (done, live proof with B) · Production monitoring ready (done; human notification channel pending) · separate founder Production activation.

## Daniel's review

Recorded as an architecture/security review contribution (integration notes). No backend contract change follows; membership/admission ownership unchanged.
